--- a/src/source_evidence.py
+++ b/src/source_evidence.py
@@
-        if observations:
-            ranked = []
-            for c in valid:
-                pos, neg, detail = _candidate_evidence(c, observations)
-                c["_evidence_positive"] = pos
-                c["_evidence_negative"] = neg
-                c["_evidence_detail"] = detail
-                ranked.append(c)
-
-            best_positive = max((c["_evidence_positive"] for c in ranked), default=0)
-            if best_positive > 0:
-                winners = [
-                    c for c in ranked
-                    if c["_evidence_positive"] == best_positive
-                    and c["_evidence_negative"] == 0
-                ]
-                if not winners:
-                    winners = [c for c in ranked if c["_evidence_positive"] == best_positive]
-                winner = max(
-                    winners,
-                    key=lambda c: (
-                        -int(c.get("_evidence_negative", 0)),
-                        float(c.get("horizon_hours", 0)),
-                        -int(c.get("priority", 999999)),
-                    ),
-                )
-                channels[channel_name] = {
-                    "source": winner.get("source"),
-                    "source_id": winner.get("source_id"),
-                    "positive_observations": int(winner["_evidence_positive"]),
-                    "negative_observations": int(winner["_evidence_negative"]),
-                    "verified_at": datetime.now(timezone.utc).isoformat(),
-                    "evidence": winner.get("_evidence_detail", []),
-                }
-                _save_state(state)
-                winner["_evidence_selected"] = True
-                return winner
-
-        learned = channels.get(channel_name) if channel_name else None
-        if isinstance(learned, dict):
+        if observations:
+            ranked = []
+            for c in valid:
+                pos, neg, detail = _candidate_evidence(c, observations)
+                c["_evidence_positive"] = pos
+                c["_evidence_negative"] = neg
+                c["_evidence_detail"] = detail
+                ranked.append(c)
+
+            # Timestamped live evidence is authoritative. A donor contradicted
+            # by any observation must not win because of policy order or a long
+            # horizon.
+            clean = [c for c in ranked if c["_evidence_negative"] == 0]
+            best_positive = max(
+                (c["_evidence_positive"] for c in clean),
+                default=0,
+            )
+            if best_positive > 0:
+                winners = [
+                    c for c in clean
+                    if c["_evidence_positive"] == best_positive
+                ]
+                winner = max(
+                    winners,
+                    key=lambda c: (
+                        float(c.get("horizon_hours", 0)),
+                        -int(c.get("priority", 999999)),
+                    ),
+                )
+                channels[channel_name] = {
+                    "source": winner.get("source"),
+                    "source_id": winner.get("source_id"),
+                    "positive_observations": int(winner["_evidence_positive"]),
+                    "negative_observations": 0,
+                    "verified_at": datetime.now(timezone.utc).isoformat(),
+                    "evidence": winner.get("_evidence_detail", []),
+                }
+                _save_state(state)
+                winner["_evidence_selected"] = True
+                return winner
+
+            # Fail closed. Once real observations exist, policy order/horizon
+            # alone are not sufficient proof of identity. This also prevents a
+            # previously learned winner from returning after new contradictory
+            # evidence appears.
+            learned = channels.get(channel_name)
+            if isinstance(learned, dict):
+                learned_candidate = next(
+                    (
+                        c for c in ranked
+                        if c.get("source") == learned.get("source")
+                        and c.get("source_id") == learned.get("source_id")
+                    ),
+                    None,
+                )
+                if (
+                    learned_candidate is not None
+                    and learned_candidate.get("_evidence_negative", 0) > 0
+                ):
+                    channels[channel_name] = {
+                        **learned,
+                        "status": "conflict",
+                        "conflict_at": datetime.now(timezone.utc).isoformat(),
+                        "evidence": learned_candidate.get("_evidence_detail", []),
+                    }
+                    _save_state(state)
+            return None
+
+        learned = channels.get(channel_name) if channel_name else None
+        if isinstance(learned, dict) and learned.get("status") != "conflict":
             for c in valid:
                 if (
                     c.get("source") == learned.get("source")
@@
         return original_choose(candidates, target_hours)
--- a/tests/test_v1518_source_evidence.py
+++ b/tests/test_v1518_source_evidence.py
@@
 def test_install_appends_evidence_candidates(monkeypatch,tmp_path):
@@
     assert rows[0]["playlist_name"]=="BCU VHS HD"
     assert rows[0]["source"]=="a"
+
+
+def _install_for_choice(monkeypatch, tmp_path, observation_title):
+    t = datetime(2026, 9, 5, 2, 10, tzinfo=timezone.utc)
+    obs = tmp_path / "obs.csv"
+    obs.write_text(
+        "enabled,playlist_name,observed_at,observed_title,notes\\n"
+        f"1,BCU VHS HD,{t.isoformat()},{observation_title},test\\n",
+        encoding="utf-8",
+    )
+    state = tmp_path / "state.json"
+    monkeypatch.setattr(ev, "OBS_PATH", obs)
+    monkeypatch.setattr(ev, "STATE_PATH", state)
+    monkeypatch.setattr(ev, "_INSTALLED", False)
+    mod = SimpleNamespace(
+        _read_policy=lambda path=None: [],
+        choose_candidate=lambda c, target_hours=6.0: c[0] if c else None,
+    )
+    ev.install(mod)
+    return mod, t, state
+
+
+def test_evidence_beats_policy_order_and_longer_horizon(monkeypatch, tmp_path):
+    mod, t, _ = _install_for_choice(monkeypatch, tmp_path, "Адвокат дьявола")
+    candidates = [
+        {
+            "playlist_name": "BCU VHS HD",
+            "source": "wrong-first",
+            "source_id": "wrong",
+            "priority": 0,
+            "usable": 20,
+            "horizon_hours": 240.0,
+            "programmes": [
+                _p("Водный мир", t-timedelta(hours=1), t+timedelta(hours=1))
+            ],
+        },
+        {
+            "playlist_name": "BCU VHS HD",
+            "source": "verified-second",
+            "source_id": "right",
+            "priority": 1,
+            "usable": 2,
+            "horizon_hours": 8.0,
+            "programmes": [
+                _p(
+                    "Адвокат дьявола (1997)",
+                    t-timedelta(hours=1),
+                    t+timedelta(hours=1),
+                )
+            ],
+        },
+    ]
+
+    winner = mod.choose_candidate(candidates, 6.0)
+
+    assert winner["source"] == "verified-second"
+    assert winner["_evidence_selected"] is True
+    assert winner["_evidence_positive"] == 1
+    assert winner["_evidence_negative"] == 0
+
+
+def test_contradictory_observation_blocks_learned_winner(monkeypatch, tmp_path):
+    mod, t, state = _install_for_choice(monkeypatch, tmp_path, "Адвокат дьявола")
+    state.write_text(
+        '{"channels":{"BCU VHS HD":{"source":"old-learned","source_id":"old",'
+        '"positive_observations":1,"negative_observations":0}}}',
+        encoding="utf-8",
+    )
+    candidates = [
+        {
+            "playlist_name": "BCU VHS HD",
+            "source": "old-learned",
+            "source_id": "old",
+            "priority": 0,
+            "usable": 20,
+            "horizon_hours": 240.0,
+            "programmes": [
+                _p("Водный мир", t-timedelta(hours=1), t+timedelta(hours=1))
+            ],
+        }
+    ]
+
+    assert mod.choose_candidate(candidates, 6.0) is None
+
+    import json
+    saved = json.loads(state.read_text(encoding="utf-8"))
+    assert saved["channels"]["BCU VHS HD"]["status"] == "conflict"
+
+
+def test_learned_winner_is_reused_without_new_observations(monkeypatch, tmp_path):
+    obs = tmp_path / "obs.csv"
+    obs.write_text(
+        "enabled,playlist_name,observed_at,observed_title,notes\\n",
+        encoding="utf-8",
+    )
+    state = tmp_path / "state.json"
+    state.write_text(
+        '{"channels":{"BCU VHS HD":{"source":"learned","source_id":"right",'
+        '"positive_observations":2,"negative_observations":0}}}',
+        encoding="utf-8",
+    )
+    monkeypatch.setattr(ev, "OBS_PATH", obs)
+    monkeypatch.setattr(ev, "STATE_PATH", state)
+    monkeypatch.setattr(ev, "_INSTALLED", False)
+    mod = SimpleNamespace(
+        _read_policy=lambda path=None: [],
+        choose_candidate=lambda c, target_hours=6.0: c[0] if c else None,
+    )
+    ev.install(mod)
+
+    candidates = [
+        {
+            "playlist_name": "BCU VHS HD",
+            "source": "policy-first",
+            "source_id": "wrong",
+            "priority": 0,
+            "usable": 20,
+            "horizon_hours": 240.0,
+            "programmes": [],
+        },
+        {
+            "playlist_name": "BCU VHS HD",
+            "source": "learned",
+            "source_id": "right",
+            "priority": 1,
+            "usable": 2,
+            "horizon_hours": 8.0,
+            "programmes": [],
+        },
+    ]
+
+    winner = mod.choose_candidate(candidates, 6.0)
+    assert winner["source"] == "learned"
+    assert winner["_evidence_selected"] is True
