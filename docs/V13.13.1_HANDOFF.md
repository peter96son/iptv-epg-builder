# v13.13.1 — CPS offset test hotfix

GitHub Actions #75 failed because two regression tests still encoded the old
CPS USSR +14h hypothesis after the production rule had been disabled.

This hotfix updates only the stale tests. No production code, metadata database,
cache, source selection, or archive retention behavior is changed.
