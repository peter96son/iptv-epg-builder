# v13.15 combined

- `openbox-tsd/cps-*`: +120 minutes to XMLTV start and stop.
- New personal playlist group `USSR`: exactly 20 agreed Soviet-film channels.
- Provider group `Кино UA` is removed from the private playlist.
- Playlist personalization is configured in `data/playlist_rules.json`.
- Stream URLs and channel block attributes including catch-up, `tvg-rec` and
  `timeshift` are preserved by the Worker.
- Old CPS USSR +14h rule remains disabled.
