# v13.20.1 manifest

This is v13.20 plus one correction: `Fresh Vhs` -> `Fresh VHS` in `data/source_pins.csv`.

Install over repository root and run Update EPG again.
Worker redeploy is NOT required if v13.20 Worker 2.2.0 was already deployed.
